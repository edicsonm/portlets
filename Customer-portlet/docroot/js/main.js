/*AUI.add('CustomerUtil', function(A){
	A.namespace('CustomerUtil');
	
	A.CustomerUtil.setPortletNamespace = function(pns){
		portletNamespace = pns;
	};
	
	A.CustomerUtil.hideTrialOptions = function(){
		if(A.one('#'+portletNamespace+'trialOptionCheckbox').attr('checked')) {
			A.one("#divTrial").set('hidden',false);
			
		}else{
			A.one("#divTrial").set('hidden',true)
		}
	};
}
);
*/